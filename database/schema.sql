-- database/schema.sql
-- Cấu trúc database (DDL) cho hệ thống Golden Lotus Restaurant Reservation.
-- Sẽ triển khai đầy đủ ở Phase P3: bảng users, tables, time_slots, bookings,
-- kèm khoá ngoại (FOREIGN KEY), chỉ mục (INDEX), và ràng buộc chống trùng lịch
-- (VD: UNIQUE trên (table_id, booking_date, time_slot_id) lọc theo trạng thái chưa huỷ).

-- TODO (Phase P3): CREATE DATABASE / CREATE TABLE ... đầy đủ sẽ nằm ở đây.
