<?php
/**
 * includes/helpers.php
 * Các hàm dùng chung: validate dữ liệu, hiển thị flash message, sinh/kiểm tra CSRF token,
 * và hàm rút gọn để escape dữ liệu ra HTML (chống XSS).
 *
 * Sẽ triển khai ở Phase P4b: e(), set_flash(), get_flash(), csrf_token(), csrf_verify(), v.v.
 */

// TODO (Phase P4b): các hàm helper dùng chung cho toàn bộ ứng dụng sẽ nằm ở đây.
